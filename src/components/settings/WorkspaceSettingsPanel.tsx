import OrganizationSettings from "@/pages/OrganizationSettings";

/** Inline company profile — identity, branding, subscription (no extra redirect). */
const WorkspaceSettingsPanel: React.FC = () => <OrganizationSettings embedded />;

export default WorkspaceSettingsPanel;
