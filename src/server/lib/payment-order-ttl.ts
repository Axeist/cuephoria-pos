/** Re-export shared checkout window (slot hold + payment_orders expiry). */
export {
  PAYMENT_CHECKOUT_PENDING_MS as PAYMENT_ORDER_PENDING_TTL_MS,
  PAYMENT_CHECKOUT_TTL_MINUTES,
} from "../../lib/payment-checkout-ttl.js";
