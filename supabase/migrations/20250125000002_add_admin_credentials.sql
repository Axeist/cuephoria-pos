-- Add/Update Admin Credentials
--
-- SECURITY NOTE:
-- Do NOT seed hardcoded admin passwords or PINs in migrations.
-- Create admin users out-of-band (Supabase SQL Editor / secure admin tooling),
-- and store only hashed secrets.
--
-- This migration is intentionally a no-op now.
SELECT 1;

